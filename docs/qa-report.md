# تقرير ضمان الجودة النهائي

**المشروع:** منظومة متابعة أعمال اللجان
**الفرع:** `codex/simplify-task-workflow`
**تاريخ التدقيق:** 2026-08-21
**الحكم المختصر:** ناجح وظيفيًا وجاهز للمراجعة في Pull Request، مع ملاحظة وصول متوسطة في قوالب Word وملاحظة أداء منخفضة. لا توجد عيوب P0 أو P1، ولا يجوز الدمج أو النشر قبل موافقة المستخدم الصريحة.

## نطاق التدقيق ومنهجيته

أُجري التدقيق على النسخة المحلية في `task-site` بصورة مستقلة، وشمل البيانات المصدرية، التقويم، الواجهة الفعلية، روابط القوالب، ملفات Office، الخصوصية، وسلامة البناء. لم تُعدّل ملفات التطبيق أو القوالب أثناء هذا التدقيق.

| المجال | النتيجة | الدليل المختصر |
|---|---|---|
| بيانات المهام والأدلة والقوالب | ناجح | 268 مهمة، 64 نوعًا مركزيًا، 19 دليلًا، 14 قالبًا، و906 تحققات آلية ناجحة |
| عدم التكرار واستقلال اللجان | ناجح | لا تكرار مطابق أو تكرار داخل القسم/اللجنة/العنوان/الأسبوع؛ اللجان التخصصية ولجنة الجداول مستقلة |
| التقويم والمصادر | ناجح مع قيد معلن | لا تداخل مع الاختبارات، ومصدر وحالة تحقق لكل حدث؛ التقويم موسوم بوضوح كمرجع مقارن غير معتمد للطائف |
| الواجهة العربية وRTL | ناجح | اختبار فعلي على 1440×900 و390×844 و320×568 |
| الخصوصية والحوكمة | ناجح | لا بريد أو هاتف في المحتوى العام أو القوالب، ولا تكليف لمتعاون خارجي |
| تنزيل القوالب | ناجح | 14/14 رابطًا محليًا يعمل ويعيد HTTP 200 ويحمل خاصية التنزيل |
| Word | ناجح بصريًا وبنيويًا؛ ملاحظة وصول | 6 ملفات سليمة و24 صفحة مرندرة بلا قص أو تداخل؛ لا عيوب وصول عالية، مع ملاحظة P2 أدناه |
| Excel | ناجح | 8 ملفات و21 ورقة؛ لا أخطاء صيغ ظاهرة ولا أخطاء `#REF!` أو `#DIV/0!` أو `#VALUE!` أو `#NAME?` أو `#N/A` |
| البناء | ناجح مع تحذير | `npm run check:data` و`npm run build` ناجحان؛ تحذير حجم حزمة P3 |

## نتائج البيانات وعدم التكرار

- نجح `npm run check:data`: **268 مهمة، 64 نوعًا مركزيًا، 19 دليلًا، 14 قالبًا، و906 تأكيدات**.
- معرّفات المهام فريدة: 268/268، ولا توجد سجلات متطابقة بعد استبعاد المعرّف.
- لا توجد مجموعات مكررة بالمفتاح `القسم + اللجنة + العنوان + الأسبوع`، ولا بالمفتاح `القسم + اللجنة + العنوان`.
- تكرار بعض العناوين بين الأقسام تكرار مقصود ومفسر بالأنواع المركزية: كل نوع من الأنواع الـ64 مرتبط بأربع أو ست نسخ تنفيذية، وليس شرحًا مكررًا وهميًا.
- جميع المهام الـ268 مرتبطة بدليل مركزي، وجميع الأدلة الـ19 مستخدمة؛ يظهر القالب في الواجهة عند الحاجة فقط.
- توزيع الأقسام: القراءات 70، الثقافة الإسلامية 64، الشريعة 70، الأنظمة 64.
- لجنة الجداول مستقلة وبها 16 مهمة.
- لجان فحص الخطط العلمية الأربع مستقلة، ولكل منها 6 مهام:
  - تخصص القراءات.
  - تخصص الدراسات القرآنية.
  - تخصص الفقه.
  - تخصص أصول الفقه.

## نتائج التقويم

- ملف الإعداد الوحيد `src/config/academicCalendar.json` يدعم الفصل الأول والثاني والصيفي، ويضع الصيفي في وضع مرجعي من دون خطة لجان محلية كاملة.
- عنوان التقويم ظاهر حرفيًا: **«تقويم جامعي رسمي مقارن — بانتظار تقويم جامعة الطائف»**، مع `reviewedAt = 2026-08-21` و`reviewDueAt = 2026-09-01`.
- كل حدث يملك نطاق تاريخ صحيحًا، وجهة إصدار، وعنوان مصدر، ورابطًا، وموضع استشهاد، وتاريخ اطلاع، وحالة تحقق، وموعد مراجعة.
- التحقق المستقل من [تقويم جامعة جدة](https://www.uj.edu.sa/ar/%D8%A7%D9%84%D8%AA%D9%82%D9%88%D9%8A%D9%85-%D8%A7%D9%84%D8%AC%D8%A7%D9%85%D8%B9%D9%8A) و[تقويم جامعة الأمير سطام](https://www.psau.edu.sa/ar/academic-calendar-table) أكد بدايات ونهايات الفصول وتواريخ الاختبارات المستخدمة كمرجع مقارن. [صفحة جامعة الطائف المتاحة للعامة](https://dgs.tu.edu.sa/academic-calendar) تعرض تقويم الدراسات العليا للفصل الثاني 1447هـ فقط؛ لذلك وسم عدم الاعتماد المحلي صحيح.
- الفصل الأول: 15 أسبوعًا تشغيليًا؛ ينتهي الأسبوع 15 في 2026-12-19، وتبدأ الاختبارات في 2026-12-20. الموعد والمهلة في الأسبوع الأخير مقيدان إلى 2026-12-17. عدد حالات التداخل: صفر.
- الفصل الثاني: 14 أسبوعًا تشغيليًا فعليًا بعد استثناء التوقفات؛ ينتهي الأسبوع 14 في 2027-05-29، وتبدأ الاختبارات في 2027-05-30. الموعد والمهلة في الأسبوع الأخير مقيدان إلى 2027-05-27. عدد حالات التداخل: صفر.
- أسبوع التهيئة منفصل، وأول مهمة تشغيلية تبدأ بعده. الإجازات التي توقف العمل تُستثنى، والمهلة تحسب بأيام العمل وتُقيد قبل الاختبارات.

## نتائج الخصوصية والحوكمة

- لم يظهر بريد إلكتروني أو رقم جوال سعودي في `src` أو الكتالوج أو الأدلة أو الإحالات أو ملفات Office الأربعة عشر.
- حقلا المنشئ وآخر معدل فارغان في ملفات Word، ولا توجد `docProps/custom.xml` أو معرفات جلسات مراجعة في أجزاء المستند/الرؤوس/التذييلات.
- تقرير الكتالوج يرصد **صفر تكليفات لمتعاونين خارجيين**.
- الأدلة تذكر صراحة أن المدعو أو الخبير الخارجي ليس عضوًا، ولا يدخل في النصاب أو التصويت، ولا يتولى التنفيذ أو المراجعة أو الاعتماد.
- حالة التسليم منفصلة عن الحالة الزمنية، وتظهر «بانتظار الربط» بدل اختلاق نسب إنجاز أو تسليم فعلية.
- لم تُرسل أي رسالة أو بريد أو إشعار أثناء العمل.

## نتائج قوالب Office

### Word

- جميع ملفات DOCX الستة حزم OOXML سليمة (`unzip -t` ناجح).
- روجعت صور الرندر النهائية لجميع الصفحات: 6 ملفات × 4 صفحات = 24 صفحة. لم يظهر قص أو تداخل أو نص خارج الصفحة، واتجاه العرض عربي RTL.
- القوالب تحتوي تعليمات داخلية، حقول تعبئة، جداول تحليل ومتابعة، مسؤولًا وموعدًا وحالة، واعتمادًا وقائمة تحقق وعلاقة بالدراسة الذاتية.
- فحص الوصول الآلي: **0 ملاحظات High** في كل الملفات. سُجلت 11–12 ملاحظة Medium لكل ملف لعدم وسم صفوف الجداول كرؤوس؛ موثقة في P2 أدناه.

### Excel

- جميع ملفات XLSX الثمانية حزم OOXML سليمة.
- روجعت صور 21 ورقة عمل، بما فيها التعليمات، البيانات، الملخصات، التحليل، وقوائم الفحص. لا قص أو تداخل ظاهر.
- فحص المصنفات والصيغ لم يعثر على أخطاء Excel القياسية، كما أن الصيغ العابرة للأوراق تقتبس أسماء الأوراق العربية بصورة صحيحة.
- المساحات الفارغة الطويلة في بعض الأوراق صفوف إدخال مقصودة وليست فقدًا للبيانات.

## نتائج الواجهة الفعلية

| القياس | السيناريوهات المنفذة | النتيجة |
|---|---|---|
| 1440×900 | دخول `١٤٢٩`، بحث `QRA`، فلاتر القسم/اللجنة، مسح الفلاتر، المهمة التالية، نافذة المهمة، الإغلاق بـESC، مكتبة القوالب، رابط SharePoint، سجل المتصفح | ناجح؛ `QRA` يعيد 70 مهمة، وفلتر لجنة فحص خطط القراءات يعيد 6، ومسح الكل يعيد 268 |
| 390×844 | القائمة المتنقلة، SharePoint، البحث والفلاتر، نافذة المهمة، ESC، حدود الشاشة | ناجح؛ عناصر اللمس الأساسية 44×44 بكسل، والنافذة داخل العرض، ولا overflow أفقي |
| 320×568 | شاشة دخول جديدة بعد إصلاح الشاشات القصيرة، دخول `1429`، الرأس، المهمة التالية، القوالب، SharePoint، النوافذ | ناجح؛ الزر بين y=452 و498، والملاحظة حتى y=544، والبطاقة حتى y=563 داخل 568، ولا overflow أفقي |

تفاصيل إضافية:

- العربية و`dir="rtl"` صحيحتان في بوابة الدخول والتطبيق.
- مكتبة القوالب تعرض 14 رابط تنزيل محليًا، ولكل رابط خاصية `download` وتسمية وصول عربية. اختُبرت الروابط كلها وأعادت HTTP 200.
- نافذة المهمة تعرض افتراضيًا المطلوب والمنفذ والموعد وثلاث خطوات وشاهدًا واحدًا، وتطوي التفاصيل الإضافية. زر ESC يغلق نوافذ المهمة والقالب.
- رابط SharePoint الصحيح ظاهر على الحاسب والجوال: `https://taifedusa.sharepoint.com/sites/CommitteeQuality`.
- سجل المتصفح النهائي يحتوي رسائل Vite وReact المعلوماتية فقط؛ لا تحذيرات ولا أخطاء JavaScript.
- لم يظهر overflow أفقي على أي من القياسات الثلاثة. التمرير العمودي الطويل في صفحات البيانات سلوك متوقع.

## أوامر التحقق النهائية

```text
npm run check:data  → ناجح (906 تأكيدات)
npm run build       → ناجح
```

مخرجات البناء الأساسية:

```text
CSS: 40.08 kB (gzip 8.05 kB)
JS:  634.51 kB (gzip 124.33 kB)
```

## العيوب والملاحظات ذات الأولوية

### P2 — صفوف رؤوس جداول Word غير موسومة دلاليًا

**النطاق:** ملفات DOCX الستة في `public/templates`.
**الأثر:** العرض والطباعة سليمان، لكن قارئ الشاشة قد لا يتعرف على رؤوس الجداول أو يكررها بصورة صحيحة. توجد جداول بيانات فعلية ذات صف أول عنواني، إلى جانب جداول تخطيط لا ينبغي وسمها آليًا كلها.
**مسار إعادة الإنتاج:** شغّل أداة `a11y_audit.py` الخاصة بمهارة المستندات على أي ملف DOCX؛ تظهر 11 أو 12 نتيجة `table_no_header_row` بدرجة Medium، مع 0 High. مثال:

```text
python /Users/majd/.codex/plugins/cache/openai-primary-runtime/documents/26.819.11345/skills/documents/scripts/a11y_audit.py public/templates/قالب_محضر_اجتماع_لجنة.docx
```

**التوصية:** وسم الصف الأول في جداول البيانات الحقيقية فقط بـ`w:tblHeader`، وعدم تطبيق الإصلاح على جداول التخطيط أو بيانات المفتاح/القيمة، ثم إعادة الرندر والفحص البصري.
**الحجب:** لا يحجب الاستخدام المرئي أو فتح PR، لكنه ينبغي أن يُعالج قبل اعتماد القوالب كوثائق متوافقة مع الوصول.

### P3 — حزمة JavaScript أكبر من حد التحذير

**النطاق:** بناء Vite.
**الأثر:** دين أداء محتمل في التحميل الأول، دون أثر وظيفي أو خطأ وقت التشغيل؛ الحجم المضغوط 124.33 kB.
**مسار إعادة الإنتاج:** شغّل `npm run build` ولاحظ تحذير أن الحزمة المصغرة أكبر من 500 kB.
**التوصية:** فصل مكتبة الأيقونات أو أقسام البيانات الثقيلة باستخدام `dynamic import()` عند الحاجة، مع قياس الأداء قبل وبعد.
**الحجب:** غير حاجب.

## الحكم النهائي

المنظومة **تجتاز القبول الوظيفي والبياني والبصري**: كل مهمة مرتبطة بدليل وقالب، لا توجد ازدواجية وهمية داخل النطاق التشغيلي، اللجان الحساسة مستقلة، التقويم لا يتداخل مع الاختبارات وموسوم بمصدره وحدود اعتماده، القوالب تعمل محليًا، والواجهة سليمة على الحاسب والجوال من دون أخطاء متصفح.

الحكم هو **نجاح مع ملاحظة وصول متوسطة غير وظيفية**. يمكن إنشاء Pull Request وعرض الملخص على المستخدم، مع إبقاء الدمج إلى `main` والنشر إلى Netlify متوقفين حتى موافقته الصريحة. كما يجب استبدال التقويم المقارن فور نشر تقويم بكالوريوس معتمد من جامعة الطائف، وعدم تقديم التواريخ الحالية على أنها اعتماد محلي.

## ملحق: تدقيق التبسيط

**نطاق اللقطة:** التغييرات المحلية الحالية الخاصة بتقليل تفاصيل المهمة، بتاريخ 2026-08-21.

**الحكم الحالي:** ينجح التحقق البنيوي والبياني والبناء والاختبار الفعلي على الحاسب والجوال بلا عيوب P0 أو P1.

### التغييرات التي روجعت

- شمل `git diff` عشرة ملفات: `README.md`، ودليلي التنفيذ والقوالب، ومدقق البيانات، و`App.tsx`، و`data.ts`، وملفات الكتالوج والإحالات والأدلة، و`styles.css`.
- التغيير الوظيفي الرئيس يستبدل نافذة المهمة الطويلة بملخص سريع، ويطوي التفاصيل الاختيارية والفلاتر الثانوية.
- لم تتغير ملفات Office الثنائية في نطاق التبسيط.

### نتائج البيانات والبناء الحالية

| التحقق | النتيجة |
|---|---|
| عدد المهام | 268/268 |
| عدد الأدلة المركزية | 19 دليلًا فريدًا، وجميعها مستخدمة |
| خطوات الأدلة | 3 خطوات لكل دليل؛ ضمن شرط 2–4 |
| الشواهد | شاهد واحد لكل دليل |
| إحالات المهام | 268/268 |
| `npm run check:data` | ناجح؛ 906 تأكيدات |
| `npm run build` | ناجح |
| ناتج CSS | 40.08 kB؛ مضغوط 8.05 kB |
| ناتج JavaScript | 634.51 kB؛ مضغوط 124.33 kB |

ما زال Vite يصدر تحذير الحزمة الأكبر من 500 kB؛ وهو استمرار لملاحظة الأداء P3 السابقة، وليس عيبًا وظيفيًا.

### مهام الساعات المكتبية T001

- المهام `QRA-T001` و`CUL-T001` و`SHR-T001` و`LAW-T001` مرتبطة كلها بالدليل المستقل `guide-office-hours-publication`.
- لكل مهمة ثلاث خطوات مطابقة للمطلوب: توقيع العضو لجدوله، تجميع المنسق والتحقق من الاكتمال، ثم اعتماد الجدول العام ونشره.
- المخرج والشاهد الكافي هما الجدول العام المنشور للساعات المكتبية، ولا توجد قوالب مرافقة.
- تحتفظ الإحالة داخليًا بمعرّف قالب أساسي لضمان سلامة نموذج البيانات، لكن `quickTemplateRequired=false` يمنع ظهور بطاقة تنزيل القالب أو زر تنزيله.
- لا تُنشئ نافذة T001 عنصر `advanced-task-details` أصلًا؛ وتعرض بدلًا منه أن المهمة لا تحتاج قالبًا إضافيًا وأن الشاهد المطلوب هو الجدول العام المنشور فقط.

### البطاقات والنوافذ والفلاتر

- بطاقة القائمة المختصرة تطابق معيار القبول المحدد: عنوان المهمة بوصفه المطلوب، اللجنة والقسم، الموعد وحالته، شاهد واحد، وزر «كيف أنجزها؟».
- كل المهام غير T001 تحتوي ثلاث خطوات، وتعرض النافذة افتراضيًا: المطلوب، المنفذ، الموعد، الخطوات الثلاث، وشاهدًا واحدًا.
- يجوز في هذه المهام ظهور بطاقة القالب وملخص «تفاصيل إضافية عند الحاجة». عنصر `advanced-task-details` لا يحمل خاصية `open`، ولذلك تكون التفاصيل مطوية بنيويًا عند الإنشاء.
- الفلاتر الأساسية الظاهرة هي البحث والقسم واللجنة. الفلاتر الثانوية الأربعة موجودة داخل `advanced-filters` بلا خاصية `open`، فتبدأ مطوية بنيويًا.
- أثبت فحص `innerText` الفعلي أن النافذة الافتراضية تستبعد محتوى التفاصيل المطوية. وفي مهمة `QRA-T003` ظهر المطلوب والمنفذ والموعد وثلاث خطوات والشاهد والقالب المساعد وملخص التفاصيل فقط.
- فُتح قسم التفاصيل الاختياري ثم طُوي مرة أخرى بنجاح، وظلت حالة `open` مغلقة افتراضيًا.

### التحقق المتجاوب الحالي

| القياس | ما تم التحقق منه | النتيجة |
|---|---|---|
| 1440×900 | نافذة `QRA-T001`، الخطوات الثلاث، غياب القالب والتفاصيل الإضافية، ومهمة `QRA-T003` ذات القالب والتفاصيل المطوية | النافذة داخل الشاشة بعرض 760 وارتفاع 850 بكسل، ولا overflow أفقي أو أخطاء متصفح |
| 390×844 | نافذتا T001 ومهمة ذات قالب، اتجاه RTL، التمرير الداخلي، وأهداف اللمس | عرض النافذة 359 بكسل، و`scrollWidth = clientWidth = 344`، والأزرار الإجرائية بارتفاع 44 بكسل فأكثر |
| 320×568 | نافذة T001 وبطاقة القائمة ذات العمود الواحد | عرض النافذة 289 بكسل وارتفاعها 552، وعرض المستند 320 دون overflow؛ أصغر هدف لمس 44 بكسل |

- في شاشة 320 بكسل بلغ عرض بطاقة المهمة 284 بكسل، وبقيت جميع عناصرها داخلها بعرض 250 بكسل.
- كانت الفلاتر الثانوية مطوية افتراضيًا، وبقي `advanced-task-details.open === false` حتى فتحه صراحة.
- سجل وحدة التحكم النهائي خالٍ من التحذيرات والأخطاء في المقاسات الثلاثة.

### ملاحظات هذه الجولة

- `git diff --check` نظيف في اللقطة النهائية.
- لا توجد ملاحظة P0 أو P1 جديدة.

#### اكتمال تغطية التبسيط

أُغلقت فجوة الاختبار المرئي بجولة متصفح فعلية شملت نافذة T001 ومهمة مركبة، و`innerText`، وحالة التفاصيل، والـoverflow، وأهداف اللمس، وسجل وحدة التحكم.
